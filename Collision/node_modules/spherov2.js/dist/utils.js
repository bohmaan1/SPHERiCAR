"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
/**
 * Wraps the passed function into a promise
 */
exports.toPromise = (binding, fn, args) => {
    return new Promise((resolve, reject) => {
        const safeArgs = args || [];
        fn.bind(binding)(...safeArgs, (err, ...retArgs) => {
            if (err) {
                reject(err);
            }
            else {
                resolve(...retArgs);
            }
        });
    });
};
/**
 * Waits the given amount of milliseconds
 * @return promise
 */
exports.wait = (time) => new Promise(callback => setTimeout(callback, time));
exports.combineFlags = (flags) => 
// tslint:disable-next-line:no-bitwise
flags.reduce((memo, flag) => memo | flag, 0);
